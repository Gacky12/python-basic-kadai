def calculate_total(price, tax):
  # 与えられた引数priceと引数taxから消費税を加えた計算結果を変数totalに代入
  total = price + price * (tax/100)

  # returnを使用して関数の戻り値totalを返す
  return total

# 第一引数は商品の金額（円）、第二引数は消費税（％）
total_price = calculate_total(100, 10)
print(f"商品の合計金額は{total_price}円です")
