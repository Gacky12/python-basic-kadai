def calculate_total(price, tax):
  total = price + price * (tax/100)
  # 処理結果を出力
  print(f"{total}円")
  return total

# 商品の金額と消費税を入力
price = input("商品の金額を入力して下さい")
price_int = int(price)
tax = input("消費税を入力して下さい")
tax_int = int(tax)
# 第一引数は商品の金額、第二引数は消費税
calculate_total(price_int, tax_int)
